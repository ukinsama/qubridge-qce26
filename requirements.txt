# Pinned versions used in the QCE26 paper.
# For reproduction of CSV/figure outputs only, the qiskit pins are not strictly
# required (CSVs are pre-computed); they are listed for full-pipeline rerun.
pandas>=2.0
matplotlib>=3.7
numpy>=1.24
qiskit==1.3.1
qiskit-aer==0.16.0
